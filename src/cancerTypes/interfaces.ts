

export interface RiskFactor {
    factor: string;
    isDeleted?: boolean;
}

export interface ProtectiveFactor {
    factor: string;
    isDeleted?: boolean;
}

export interface ScreeningMethod {
    method: string;
    isDeleted?: boolean;
}

export interface CopingMethod {
    method: string;
    isDeleted?: boolean;
}

export interface Treatment {
    method: string;
    isDeleted?: boolean;
}